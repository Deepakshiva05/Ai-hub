services:
  backend:
    build: ./backend-fastapi
    ports:
      - "8000:8000"
    env_file:
      - ./backend-fastapi/.env
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
